package com.oao.backend.heart.repository;

import com.oao.backend.heart.domain.InstantIntroductionConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InstantIntroductionConfigRepository extends JpaRepository<InstantIntroductionConfig, Long> {
}
